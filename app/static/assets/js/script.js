const fileUploader = document.querySelector(".file-uploader");
const documentUploadStatus = document.querySelector(".documentPreview");

// fileUploader.addEventListener("change", getFileName);

// function getFileName(event) {
//   const files = event.target.files;
//   const fileName = files[0].name;
//   console.log(fileName);
//   documentUploadStatus.innerHTML = `<h4>Uploaded File:</h4>${fileName}`;
// }
